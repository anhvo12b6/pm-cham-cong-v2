# Chặn các file chứa mật khẩu, IP Server, JWT Secret
.env
*.env
backend/.env

# Chặn thư mục thư viện tải về (rất nặng, không cần đưa lên Git)
node_modules/
backend/node_modules/
frontend/node_modules/

# Chặn thư mục build sản phẩm của React
frontend/dist/
frontend/build/

# Chặn file log hệ thống
*.log
npm-debug.log*